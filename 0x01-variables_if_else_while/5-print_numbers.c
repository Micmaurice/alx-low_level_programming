#include <stdio.h>

/**
 * main - prints out base ten numbers
 * @void: parameters for main
 * Return: 0 for success
 */

int main(void)

{
	char num = '0';

		while (num <= '0')
		{
			putchar(num);
			num++;
		}
	putchar('\n');
	return (0);
}
